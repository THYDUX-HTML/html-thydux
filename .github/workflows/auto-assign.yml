name: Thydux 
run-name: ${{ thydux.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-thydux-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ thydux.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by Thydux!"
      - run: echo "🔎 The name of your branch is ${{ thydux.ref }} and your repository is ${{ Thydux.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "💡 The ${{ Thydux.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ thydux.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
